spring:
  jpa:
  hibernate:
    ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/hawfinch_dev
  properties:
    hibernate:
      dialect=org:
        hibernate:
          dialect:
            PostgreSQLDialect
